<%- include('header.ejs') %>

<div class="intro">
</br>
<input type="text" id="Larecherche" onkeyup="Recherche()" placeholder="Recherchez un skin...">
</br></br>

<p>Voici les skins actuellement en vente</p>
</div>
<div class="container" id="Lesskins">

 <!--h2><%= title %></h2>-->
</br></br>
  <a href="/skin/add" class="btn btn-sm btn-primary">Mettre en vente un skin</a><br/>
  <table id="Lesskins2" class="table table-dark table-striped">
    <thead>
      <tr>
        <th>Nom du skin</th>
        <th>Jeu concerné</th>
        <th>Prix (euros)</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
        <% annonce.forEach(function(item){ %>
            <tr>
                <td><%= item.name %></td>
                <td><%= item.jeu %></td>
                <td><%= item.prix %> euros</td>

                <td>
                    <a href="/annonce/get/<%= item.id %>" class="btn btn-sm btn-primary">Voir</a>
                    <a href="/annonce/edit/<%= item.id %>" class="btn btn-sm btn-primary">Modifier</a>
                    <a href="/annonce/delete/<%= item.id %>" class="btn btn-sm btn-danger">Supprimer</a>
                    <a href="/cart/<%= item.id %>" class="btn btn-sm btn-success">Acheter</a>
                </td>
            </tr>
        <% }); %>
    </tbody>
</div>
</br>

<%- include('footer.ejs') %>